build/
__pycache__/
*.pyc

# R2 credentials — never commit. Copy .env.r2.example to .env.r2 and fill it in.
.env.r2

# Lot photographs live in Cloudflare R2, not git (2.06 GB, 5,222 files).
# Pull them with: scripts/r2.sh pull
data/*/lots/*/images/
